# Flyway abilitato
spring.flyway.enabled=true

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/epas-prod
spring.datasource.username=epas
spring.datasource.password=orario

# Impostazioni specifiche dell'applicazione

#epas.security.oauth2ResourceserverEnabled=false

# In caso sia attivata la oauth2 e' necessario configurare il parametro
# sottostante con il jwt issuer uri
# spring.security.oauth2.resourceserver.jwt.issuer-uri=https://sso.cnr.it/auth/realms/CNR
# epas.security.oauth2.jwtfield=email_cnr
# epas.security.oauth2.userAuthIdentifier=eppn